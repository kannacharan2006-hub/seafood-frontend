import 'package:flutter/material.dart';

class AppColors {

    // LIGHT MODE
      static const primary = Color(0xFF0F1C2E);
        static const secondary = Color(0xFF142B44);

          static const accentStart = Color(0xFF5EEAD4);
            static const accentEnd = Color(0xFF6366F1);

              static const luxury = Color(0xFFF5C76E);

                static const backgroundLight = Color(0xFFF9FAFB);
                  static const cardLight = Color(0xFFFFFFFF);

                    static const success = Color(0xFF1F8A70);
                      static const warning = Color(0xFFF59E0B);
                        static const error = Color(0xFFDC2626);

                          // DARK MODE
                            static const backgroundDark = Color(0xFF0B1220);
                              static const cardDark = Color(0xFF111C2F);

                                static const accentGlow = Color(0xFF5EEAD4);
                                  static const highlight = Color(0xFF8B5CF6);

                                    static const primaryTextDark = Color(0xFFE2E8F0);
                                      static const secondaryTextDark = Color(0xFF94A3B8);
                                      }
